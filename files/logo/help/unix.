unix  -- Command, one input.
     The input must be a TOPS-20 system program name optionally followed by
     a command line that is "rescanned" to the program.  The program is run
     in an inferior fork.  Example:

     to finger :user
     unix (sentence "finger :user)
     end
