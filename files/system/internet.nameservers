; This file is for DEC's resolver.  We use Chives instead, because Chives
; permits the loading of local zones and more importantly because DEC's
; resolver has some bugs with MX lookups that haven't been resolved yet.
; If this file was actually used, lines such as the following wouldn't be
; commented out.
; 10.0.0.1,TIMEOUT:5
; 10.0.0.3,TIMEOUT:5
