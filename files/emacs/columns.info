<Alt>X Make Blocks

  will format a single column with each block taking the same amount of
  vertical space. (You probably don't need this now -- see next command).

<Alt>X Make Two Column Blocks

  It will format a single column as above, decide where the middle of the
  buffer is automatically, split it into two columns and clean up redundant
  blank lines between sections ...
